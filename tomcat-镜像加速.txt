<Context docBase="F:/abc" path="/abc">  映射一个web项目--代表一个web项目

<context>标签  有一个context接口，即一个Java类与之对应
<server>标签 有一个server接口

所以配置文件的标签，一般对应着类，从而把标签里面的内容加载进来

tomcat性能优化（调优）：1，jvm内存 2.servel.xml 中connector将bio配置文nio

还可以配置线程池


docker pull registry.docker-cn.com/library/